# LogSys Knowledge Graph Frontend

这是精简后的商业图谱视图版本：知识图谱占据主体页面，只保留上传文件、刷新和清空三个操作。

```bash
pnpm install
pnpm dev
```

如果后端不是 `http://127.0.0.1:8000`，可以创建 `.env`：

```env
VITE_API_BASE=http://127.0.0.1:8000
```
